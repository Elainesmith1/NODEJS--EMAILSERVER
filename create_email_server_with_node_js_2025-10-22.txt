Here’s a concise guide to setting up an email server in Node.js using Nodemailer, a popular library for sending emails:

Step 1: Install Dependencies
Run the following command in your project directory to install Nodemailer:
Bashnpm install nodemailer


Step 2: Create the Email Server
Here’s a basic example of setting up an email server:
Javascriptconst nodemailer = require('nodemailer');

// Create a transporter
const transporter = nodemailer.createTransport({
  host: 'smtp.example.com', // Replace with your SMTP server
  port: 587,               // Common SMTP port
  secure: false,           // Use true for 465, false for other ports
  auth: {
    user: 'your-email@example.com', // Your email
    pass: 'your-email-password'     // Your email password
  }
});

// Send an email
const mailOptions = {
  from: '"Your Name" <your-email@example.com>', // Sender address
  to: 'recipient@example.com',                 // List of recipients
  subject: 'Hello from Node.js',               // Subject line
  text: 'This is a plain text email!',         // Plain text body
  html: '<b>This is an HTML email!</b>'        // HTML body
};

transporter.sendMail(mailOptions, (error, info) => {
  if (error) {
    return console.log('Error:', error);
  }
  console.log('Email sent:', info.response);
});


Step 3: Test Your Email Server

Replace smtp.example.com, your-email@example.com, and your-email-password with your SMTP provider's details (e.g., Gmail, Outlook, or a custom SMTP server).
Run the script:Bashnode your-script.js


Check the recipient's inbox to confirm the email was sent.


Optional: Use Mailtrap for Testing
If you’re testing in a development environment, you can use Mailtrap to avoid sending real emails. Update the transporter configuration with Mailtrap credentials:
Javascriptconst transporter = nodemailer.createTransport({
  host: 'smtp.mailtrap.io',
  port: 2525,
  auth: {
    user: 'your-mailtrap-username',
    pass: 'your-mailtrap-password'
  }
});


This setup is simple yet effective for sending emails in Node.js applications. Let me know if you need further assistance! 😊
